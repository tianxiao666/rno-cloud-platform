spring:
  application:
    name: rno-cloud-platform
  messages:
    basename: i18n/messages
  profiles:
    active: dev
#    include: cas

rno:
  version: 4.0.1

security:
  basic:
    enabled: false
  cas:
    enabled: false
