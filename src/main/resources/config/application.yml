spring:
  application:
    name: @project.artifactId@
  messages:
    basename: i18n/messages
  profiles:
    active: dev

info:
  app:
    name: ${spring.application.name}
    version: @project.version@
